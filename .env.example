NEXT_PUBLIC_INSTANT_APP_ID=replace_with_your_instant_app_id

# Secret per firmare i cookie di sessione (auth custom username/password).
# Genera un valore random con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=replace_with_a_random_secret
