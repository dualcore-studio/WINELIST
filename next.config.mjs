/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Indicatore di sviluppo di Next in basso a destra: a sinistra copriva il selettore IT/EN della barra.
  devIndicators: { position: "bottom-right" }
};

export default nextConfig;
